function number(value) {
    document.form.showNumber.value += value;
}
function cle(value) {
    document.form.showNumber.value = value;
}
function evalua() {
    document.form.showNumber.value = eval(document.form.showNumber.value);
}